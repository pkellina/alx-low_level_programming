#include "main.h"

/**
 * main - causes an infinite loop
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	int i;

	printf("Infinite loop incoming :(\n");

	/**
	 * i = 0
	 *
	 * while (i < 0)
	 * {
	 * 	putchar(i);
	 * }
	 */
	printsf("Infinite loop avoided! \\o/\n");
	return (0);
}
